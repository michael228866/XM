# GEMINI DUKASCOPY FX TICK GAP ADJUDICATION V1 — ABORTED ATTEMPT

Status: `aborted`

The frozen 554-gap / 12,823 pair-hour plan was created from the immutable parent. Acquisition was stopped after 400 committed first-pass responses (384 HTTP 200 and 16 errors) because the per-request `urllib` HTTPS connection setup made the required 25,646 requests operationally impractical.

No adjudication conclusion was drawn. No missing interval was classified from this partial acquisition. The SQLite response evidence, exact timestamp snapshot, native-M1 inputs, request manifest, executed script, and stdout are preserved.

No model, label, prediction, target, trade, or strategy metric was accessed. `gemini.py` and the operational model remained unchanged.
