# GOLD future capture final readiness v1

This bounded research stage preserves both schemas, v1 files, all v2 protocol
and collector rules, and prior finalized runs. It does not change attestations
without sufficient evidence. Current external evidence remains insufficient.

Official document reviews are retained in research_evidence with exact hashes,
review times, claims and limitations. Generic UTC API documentation, another
entity's GMT+2/+3 display rule and historical holiday notices cannot certify
XMGlobal-MT5 6 annual encoding or GOLD# sessions/calendar.

The diagnostic uses only initialize, shutdown, terminal_info, symbol_info,
symbol_info_tick and copy_rates_range. It never calls account_info or reads
prices from returned arrays. Six fixed historical five-minute windows and one
current window retain timestamps only. Historical timestamps lack independent
UTC anchors. Current offset support is reported separately from annual status.
Exact current server/environment cannot be reverified with these permitted
calls; archived demo evidence remains explicitly historical.

The prefix tool reads source text and computes only deterministic synthetic
EMA/MACD fixtures. No model imports, market feature computation, targets or
strategy execution occur. See the reinitialization proposal for exact limits.

Workflow: run synthetic checks and compilation; commit/push clean source;
execute run_gold_future_capture_final_readiness_v1.py --execute; run the
archived independent validator once; finalize research_only if PARTIAL and
register; provenance validate; archive commit/push. Diagnostics execute once
within the formal run. Existing v2 certification is invoked unchanged.

The runner fails closed if inputs unexpectedly become complete: a PASS path
requires an independently reviewed freeze/activation stage and cannot be
inferred from this unresolved evidence. No automatic collector launch exists.
Capture, holdout and evaluation timestamps remain null while unresolved.
