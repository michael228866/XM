# GOLD external attestation and prefix freeze readiness

The v2 source ID, exact GOLD# symbol, native 21 timeframes, no-resampling rule,
immutable chain and rebuildable tip are preserved. Neither schema nor the
collector/certifier is relaxed. No prior finalized run is changed.

New evidence reviews add publisher, title and unsupported-claim scope. An exact
XM support request is retained unsent because no authorized outbound channel is
available. Generic official Forex hours and an older Cypriot entity policy do
not certify XMGlobal-MT5 6 / GOLD# annual epoch conversion or session rules.
An official holiday publication can be a future update authority only after
exact-source applicability is reviewed; this task does not demand knowing all
future holidays in advance. Updates must be dated, hashed, outcome-independent
and append-only with no change to past sealed evidence.

Diagnostic v2 is necessary to add October/November windows and explicit sample
fields without editing historical v1 source. It requests ten fixed historical
five-minute M1 windows plus current, projects only time fields, and records
hypothetical conversion separately from actual independent UTC anchors. No
prices, account identifiers, balance/equity/margin or trade history are logged.
account_info remains outside the permitted call set; archived demo identity is
not a claim of current re-verification.

The causal-context protocol explicitly permits compatible pre-boundary native
history for warmup. It does not force 21 Monthly bars to accumulate after
activation. Source/timezone compatibility and a sealed exact context inventory
remain approval prerequisites. No history was loaded to inspect performance.
Prior synthetic characterization is copied byte-for-byte as historical evidence,
not rerun and not relabelled as exact historical equivalence.

Workflow: synthetic checks and compile; source commit/push; clean preregistered
formal run; one-shot independent validator; finalize/register; provenance
validation; archive commit/push. Production SHA256 is checked before/after each
major formal stage and retained in protection_checks.jsonl. Any mismatch stops
execution and is never silently restored.

If evidence remains unresolved, finalize PARTIAL/research_only and stop without
freeze, activation or boundary. A real PASS would require all certification and
independent gates, followed by separate frozen-protocol and activation stages.
This source does not create speculative activation artifacts or launch capture.
